% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_signif_components.R
\name{get_signif_components}
\alias{get_signif_components}
\title{Identify Significant Variables Using Logistic Regression}
\usage{
get_signif_components(datasc, levs)
}
\arguments{
\item{datasc}{A data frame with a column \code{class} (factor) for binary classification and other numeric predictor variables. Must also contain a column \code{sample} which will be excluded.}

\item{levs}{Character vector of length 2 indicating the two levels of the binary outcome variable. The second level is considered the positive class.}
}
\value{
A data frame with one row per variable including the p-value from logistic regression and metrics: Accuracy, Sensitivity, Specificity, Positive Predictive Value (PPV), and Negative Predictive Value (NPV).
}
\description{
Performs univariate logistic regressions for each variable in a dataset to determine their association with a binary outcome. Returns performance metrics for each variable.
}
\details{
For each predictor variable, a logistic regression is fit using \code{glm}. Predictions are binarized with a threshold of 0.5 and compared to the true class to generate confusion matrices. Variables are ranked by p-value.
}
\examples{
\dontrun{
if(interactive()){
  # Example usage:
  data <- data.frame(
    sample = paste0("S", 1:100),
    var1 = rnorm(100),
    var2 = rnorm(100),
    class = factor(sample(c("Control", "Case"), 100, replace = TRUE))
  )
  get_signif_components(data, levs = c("Control", "Case"))
}
}
}
\seealso{
\code{\link[dplyr]{select}},
\code{\link[dplyr]{arrange}},
\code{\link[stats]{glm}},
\code{\link[stats]{predict}},
\code{\link[caret]{confusionMatrix}}
}
