% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/WriteManyPlots.R
\name{WriteManyPlots}
\alias{WriteManyPlots}
\title{Write Multiple ggplot2 Plots to PDF Files}
\usage{
WriteManyPlots(
  plotlist,
  name,
  outdir,
  w = 12,
  h = 7,
  separate = F,
  opt = list()
)
}
\arguments{
\item{plotlist}{A named list of ggplot2 plot objects to be saved.}

\item{name}{A base name to use for the output file(s).}

\item{outdir}{The output directory where the PDF file(s) will be written.}

\item{w}{Width of the PDF plot(s), in inches. Default: \code{12}.}

\item{h}{Height of the PDF plot(s), in inches. Default: \code{7}.}

\item{separate}{Logical; if \code{TRUE}, writes each plot to a separate PDF file. If \code{FALSE}, all plots are written to a single PDF. Default: \code{FALSE}.}

\item{opt}{Optional list of options passed to \code{getNullPlot()} in case a plot fails. Default: \code{list()}.}
}
\value{
No return value. Side effect: generates PDF files with the specified plots.
}
\description{
Saves a list of ggplot2 plots to individual PDF files or a single combined PDF.
}
\details{
If a plot in the list fails to render, a fallback plot generated by \code{getNullPlot(opt, plotname)} is printed instead.
The function creates the output directory if it does not exist.
}
\examples{
\dontrun{
if(interactive()){
 # Save all plots to a single PDF
  WriteManyPlots(my_plot_list, name = "analysis", outdir = "results")

  # Save each plot to its own PDF
  WriteManyPlots(my_plot_list, name = "analysis", outdir = "results", separate = TRUE)

 }
}
}
