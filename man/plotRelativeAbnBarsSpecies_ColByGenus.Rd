% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotRelativeAbnBarsSpecies_ColByGenus.R
\name{plotRelativeAbnBarsSpecies_ColByGenus}
\alias{plotRelativeAbnBarsSpecies_ColByGenus}
\title{Plot Relative Abundance Barplot of Top Species Colored by Genus}
\usage{
plotRelativeAbnBarsSpecies_ColByGenus(
  phobj,
  variable = "Condition",
  topn = 15,
  outname = "SpeciescolByGenusBarplot.pdf",
  height = 8,
  width = 12,
  docluster = T
)
}
\arguments{
\item{phobj}{A phyloseq object containing OTU counts and taxonomy data.}

\item{variable}{Sample metadata variable used for grouping/faceting (not fully implemented in this snippet). Default: "Condition"}

\item{topn}{Number of top ASVs (species) to include in the plot, sorted by total abundance. Default: 15}

\item{outname}{File name to save the resulting plot as PDF. Default: "SpeciescolByGenusBarplot.pdf"}

\item{height}{Height of the saved plot in inches. Default: 8}

\item{width}{Width of the saved plot in inches. Default: 12}

\item{docluster}{Logical; whether to cluster samples and genera to reorder the plot axes. Default: TRUE}
}
\value{
A ggplot2 object representing the stacked barplot.
}
\description{
Generates a stacked bar plot of the relative abundances of the top N most abundant ASVs (species-level),
coloring each species by its genus. The samples are optionally clustered or ranked for better visualization.
}
\details{
The function first normalizes counts to relative abundances per sample,
then selects the top N ASVs by total abundance.
It assigns unique genus names (with suffixes if needed) to distinguish genera with duplicates.
Samples and genera are optionally reordered by hierarchical clustering.
Finally, a stacked bar plot is created and saved to file.
}
\examples{
\dontrun{
if(interactive()){
  library(phyloseq)
  data(GlobalPatterns)

  # Plot top 10 species colored by genus, save to a PDF
  plotRelativeAbnBarsSpecies_ColByGenus(GlobalPatterns, variable="SampleType", topn=10,
                                       outname="example_barplot.pdf", docluster=FALSE)
}
}
}
\seealso{
\code{\link[phyloseq]{transform_sample_counts}}
\code{\link[dplyr]{filter}}, \code{\link[dplyr]{arrange}}, \code{\link[dplyr]{mutate}}, \code{\link[dplyr]{mutate_if}}
\code{\link[tidyr]{gather}}
}
